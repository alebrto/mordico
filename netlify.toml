[build]
  command = "npm run build"
  publish = "dist"

# Todas las rutas de React Router deben servir index.html,
# de lo contrario recargar /clientes, /ventas, etc. dará 404 en Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
